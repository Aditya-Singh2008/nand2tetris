CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Not(in=a, out=NotA);
    Not(in=b, out=NotB);
    And(a=a, b=NotB, out= AAndNB);
    And(a=b, b=NotA, out= BAndNA);
    Or(a=AAndNB, b=BAndNA, out=out);
}
